var express = require('express');
var ParseServer = require('parse-server').ParseServer;
var ParseDashboard = require('parse-dashboard');
var path = require('path');
var app = express();
require('dotenv').config();


const dbConn = 'mongodb+srv://techdell727:ola_12345@cluster0.iowofnp.mongodb.net/?retryWrites=true&w=majority';
const APP_ID = 'akanjiOla';
const MASTER_KEY = 'myNewMasterKeyApi';
const REST_API_KEY ='akanjiOlaKey';
const SERVER_URL = 'https://dreamserver.onrender.com/parse'
//https://dreaminvestment-b52297c0d397.herokuapp.com
const api = new ParseServer({
  databaseURI:dbConn,
  cloud:__dirname + '/cloud/main.js',
  appId:APP_ID,
  masterKey:MASTER_KEY,
  restApiKey:REST_API_KEY,
  serverURL:SERVER_URL

})

 api.start();
app.use('/parse',api.app)
//dashboard
var options = { allowInsecureHTTP: false};
var dashboard = ParseDashboard(
  {
    apps: [
      {
        serverURL:SERVER_URL,
        appId: APP_ID,
        masterKey: MASTER_KEY,
        appName: 'Q Invest'
      }
    ],
    users: [
      {
        user: 'akanjiola',
        pass: 'akanjiola'
      }
    ],
    trustProxy: 1
  },
  options
);
app.use('/dashboard', dashboard);


// routing
app.get('/', (req, res) => {
  res.send('Parse Server is up again.');
});
app.get('/test', (req, res) => {
  res.sendFile(path.join(__dirname, '/public/test.html'));
});

var port = process.env.PORT || 1337;
var httpServer = require('http').createServer(app);
httpServer.listen(port, () => {
  console.log('parse server is running on port ' + port);
});

// Live Query:
ParseServer.createLiveQueryServer(httpServer);































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































